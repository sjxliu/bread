const React = require("react")
const Default = require("./layout/Default")

function Show({bread}){
    return(
        <Default>
            <h2>Show Page</h2>
        </Default>
    )
}

module.exports = Show;